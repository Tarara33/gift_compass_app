<div class="container">
  <div class="row">
    <div class="row row-cols-1 row-cols-lg-3 row-cols-md-2 g-4">
      <% if @items.present? %>
        <%= render @items %>
      <% else %>
        <p>まだ欲しいものたちが登録されてません</p>
      <% end %>
    </div>
  </div>
</div>